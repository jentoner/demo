<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Descripton</th>
      <th>Price</th>
      <th>Stock quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= product.descripton %></td>
        <td><%= product.price %></td>
        <td><%= product.stock_quantity %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p>Total number of unique products: <%= @unique_products %>!</p>
<p>Average price of products: $<%= @average_price %>!</p>
<p>Total stock quantity: <%= @total_stock %>!</p>
</table>
  <thead>
      <th>Chunky Bacon, the table of Bacon that is Chunky</th>
  </thead>
  <tbody>
    <tr>
      <% 100.times do %><td>Chunky Bacon</td><%end%>
    </tr>
    <tbody>
  </table>
<br>

<%= link_to 'New Product', new_product_path %>
